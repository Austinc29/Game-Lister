<h1> This is the Game Edit form </h1>

<form action="/games/<%= @game.id %>" method="post">
    <input id="hidden" type="hidden" name="_method" value="patch">
    Title: <input type=text value="<%= @game.title %>" name="title">
    Release Date: <input type=text value="<%= @game.date %>" name="date">
    <input type=submit>
<form>